---
import { Image } from "astro:assets";
import Button from "./Button.astro";
import Text from "./Text.astro";
import Heading from "./Heading.astro";
import fallbackImage from "../assets/images/welcomeimage.jpg";

const {
  image = fallbackImage,
  imageAlt = "",
  heading = "Overskrift",
  text = "Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.",
  ctaLabel = "CTA primary",
} = Astro.props;
---

<div
  class="grid grid-cols-4 md:grid-cols-12 gap-2xs items-start md:items-center"
>
  <div class="col-span-4 md:col-span-5">
    <Image src={image} alt={imageAlt} class="w-full h-auto object-cover" />
  </div>

  <div class="col-span-4 md:col-span-6 md:col-start-7 text-text-dark">
    <Heading htmlheading="h2" size="medium" stylingclasses="leading-none">
      {heading}
    </Heading>

    <Text teksttype="brødtekst" stylingclasses="mt-2xs">
      {text}
    </Text>

    <div class="mt-xs">
      <Button variant="primary" size="medium">{ctaLabel}</Button>
    </div>
  </div>
</div>
